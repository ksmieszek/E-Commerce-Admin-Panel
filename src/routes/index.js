const routes = {
  root: "/",
  products: "/products",
  collections: "/collections",
  menuManagement: "/menu-management",
  photos: "/photos",
  users: "/users",
  orders: "/orders",
};

export default routes;
